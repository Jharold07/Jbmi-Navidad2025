# Navidad Regalo 🎁🎄

Landing navideña (HTML/CSS/JS) con animación de regalo (GSAP) y fuegos artificiales (Canvas).

## Cómo usar (local)
1. Abre `index.html` con Live Server (VSCode) o cualquier servidor local.
2. (Opcional) agrega tus audios:
   - `assets/audio/open-gift.mp3`
   - `assets/audio/christmas-music.mp3`

## Deploy (rápido)
- Vercel o Netlify: sube esta carpeta (o conecta el repo de GitHub) y listo.

## Personalización rápida
- Cambia el texto final en `index.html` (id `finalMsg`) o usa el botón **Editar mensaje**.
- Colores en `css/variables.css`.
